import FirebaseContext, {
  withFirebase
} from "customComponents/Firebase/context";
import Firebase from "customComponents/Firebase/firebase";

export default Firebase;

export { FirebaseContext, withFirebase };
